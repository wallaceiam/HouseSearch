import React from 'react';
// import * as d3 from 'd3';

// type RenderFunc = (svg: d3.Selection<any, unknown, null, undefined>) => void; // d3.Selection<GElement, OldDatum, null, undefined>;

// export const useD3 = (render: RenderFunc, deps?: React.DependencyList | undefined) => {
//     const ref = React.useRef<SVGSVGElement>(null);

//     React.useEffect(() => {
//         render(d3.select(ref.current));
//         return () => {};
//       }, deps);
//     return ref;
// }

export const useD3 = () => {};