{
  "name": "@rightlocation/schema",
  "version": "1.0.0",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "license": "MIT",
  "scripts": {
    "clean": "rimraf ./lib",
    "dev": "rimraf ./lib && tsc --build --watch",
    "compile": "rimraf ./lib && tsc --build"
  },
  "dependencies": {
    "mongoose": "^6.7.0"
  },
  "devDependencies": {
    "rimraf": "^3.0.2"
  }
}
